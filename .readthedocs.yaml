version: 2

build:
  os: ubuntu-24.04
  tools:
    python: "3.12"
  jobs:
    post_install:
      - |
        python - <<'PY'
        import myst_parser, sys
        print("MyST OK", myst_parser.__version__, sys.version)
        PY

sphinx:
  configuration: docs/conf.py

python:
  install:
    - requirements: docs/requirements.txt
